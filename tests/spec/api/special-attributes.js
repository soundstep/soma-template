describe("api - special attributes", function () {

	beforeEach(function () {
		createContainer();
		createTemplate();
	});

	afterEach(function () {
		disposeTemplate();
		disposeContainer();
	});

	it("special attributes", function () {
		// data-repeat and others
	});

});
